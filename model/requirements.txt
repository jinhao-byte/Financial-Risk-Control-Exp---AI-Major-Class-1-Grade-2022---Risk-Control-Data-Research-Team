# 基础业务依赖（精确版本）
requests==2.31.0          # 网络请求库
pandas==2.0.3             # 数据处理库
sqlalchemy==2.0.20        # 数据库ORM框架

# 开发环境依赖（仅本地用，可单独拆分到 dev.txt）
pytest==7.4.0             # 测试框架
black==23.10.1            # 代码格式化工具

# 引入其他依赖文件（可选）
-r ./docs_requirements.txt  # 文档生成相关依赖